#!/usr/bin/env groovy

pipeline {
    agent any

    stages {
        stage('Checkout') {

            steps {
                echo 'Checking out from SCM'

                checkout scm
            }
        
        }
    }
}
